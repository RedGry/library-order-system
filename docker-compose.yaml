services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgres'
      - 'POSTGRES_PASSWORD=testpassword'
      - 'POSTGRES_USER=testuser'
    ports:
      - '5432:5432'
